<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.islamics.estakem">

<application
      android:name=".MyApplication"
      android:hardwareAccelerated="false"
      android:icon="@mipmap/ic_launcher_round"
      android:label="@string/app_name"
      android:largeHeap="true"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".ui.MyChallengesActivity" />
    <activity android:name=".ui.EngazatActivity" />
    <activity android:name=".ui.ErshadatActivity" />
    <activity
        android:name=".ui.TarkZanbActivity"
        android:windowSoftInputMode="stateAlwaysHidden" />
    <activity android:name=".ui.ReviewActivity" />
    <activity android:name=".ui.SonanMahgoraActivity" />
    <activity
        android:name=".ui.SalaActivity"
        android:windowSoftInputMode="stateAlwaysHidden" />
    <activity android:name=".ui.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ui.FaraedActivity" />

    <receiver android:name=".MyNotificationPublisher" />
  </application>

</manifest>